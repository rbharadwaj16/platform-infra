apiVersion: storage.acme.com/v1alpha1
kind: StorageAccount              # this is the XR kind you defined in the XRD
metadata:
  name: my-storage-account
  namespace: team-alpha           # namespaced, so safe for developers
spec:
  parameters:
    storageAccountName: myapp2024
    resourceGroupName: myapp-rg
    location: eastus
    sku: Standard_LRS