apiVersion: v1
kind: Service
metadata:
  name: translator-service
  namespace: backstage
spec:
  type: ClusterIP
  selector:
    app: translator-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
