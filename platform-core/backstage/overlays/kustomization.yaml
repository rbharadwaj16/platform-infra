apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

patches:
  - path: 01-backstage-argo-app-patch.yaml
    target:
      group: argoproj.io
      version: v1alpha1
      kind: Application
      name: backstage
